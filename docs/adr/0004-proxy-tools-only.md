# Proxy MCP tools only

The first release exposes tools from MCP Connections but does not combine their resources or prompts. Tool aggregation and authorization form a coherent product boundary; partially proxying other MCP features would add naming, subscription, and policy semantics before a concrete need exists.
