# Import through the MCP Registry v0.1 API

The official MCP Registry is the default Registry Source, and administrators may add compatible private sources. Import pre-fills an unsaved Connection form; the gateway validates it before saving. Package upgrades require explicit review because registry ownership proves publisher identity, not that executable server packages are safe.
