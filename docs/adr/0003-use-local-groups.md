# Manage groups locally

The gateway manages Groups locally rather than synchronizing them from SSO claims. This keeps authorization independent of identity-provider-specific group formats and lets the same model include Users and Service Accounts, at the cost of separate membership administration.
