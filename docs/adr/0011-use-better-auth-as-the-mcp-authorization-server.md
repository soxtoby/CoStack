# Use Better Auth as the MCP authorization server

Better Auth authenticates browser Users through the configured SSO Provider and issues OAuth 2.1 tokens for the Gateway MCP. Public loopback clients such as Codex may register through Dynamic Client Registration before the User signs in and consents. Administrators create client credentials for confidential MCP Clients.
