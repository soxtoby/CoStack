# Use Better Auth as the MCP authorization server

Better Auth authenticates browser Users through the configured SSO Provider and issues OAuth 2.1 tokens for the Gateway MCP. ChatGPT registers through CIMD, while administrators create client credentials for Claude and other predefined MCP Clients; Dynamic Client Registration remains out of scope until compatibility testing proves it necessary.
